services:
  - type: web
    name: socgen-sentinel
    env: python
    buildCommand: "bash ./build.sh"
    startCommand: "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: GEMINI_API_KEY
        sync: false
